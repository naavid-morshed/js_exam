const arr = new Array(1,2,3,4,5);

for(var count = 0; count < arr.length; count++){
    console.log(arr[count]);
}

arr.reverse();

for(var count = 0; count < arr.length; count++){
    console.log(arr[count]);
}
